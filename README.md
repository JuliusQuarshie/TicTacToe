# TicTacToe
Classic two-player Tic-Tac-Toe in C++ (CLI). Players enter names and take turns marking 1–9 positions; game validates input, prevents overwriting, detects wins (rows/cols/diagonals) and draws. Compile with g++.
